# Watch round 5 report

## Files changed

- `Common/src/main/java/tk/glucodata/logic/TrendEngine.kt` — moved unchanged from `src/mobile`.
- `Common/src/main/java/tk/glucodata/logic/TrendEngineVelocityProvider.kt` — moved from `src/mobile` so Wear can register the shared engine on the current provider-based `TrendAccess` implementation.
- `Common/src/main/java/tk/glucodata/ui/GlucosePoint.kt` — extracted the existing FQN data model from mobile-only `GlucoseDisplay.kt` for the shared engine.
- `Common/src/main/java/tk/glucodata/ui/util/GlucoseFormatter.kt` — moved from `src/mobile` because `TrendEngine` depends on its conversion constant.
- `Common/src/mobile/java/tk/glucodata/ui/GlucoseDisplay.kt` — removed only the extracted `GlucosePoint` declaration.
- `Common/src/main/java/tk/glucodata/TrendAccess.kt` — removed the stale fallback return that discarded a successfully resolved provider value.
- `Common/src/main/java/tk/glucodata/ui/components/TrendArrowCanvas.kt` — now uses `TrendArrowAngle.rotationDegrees(velocity)`.
- `Common/src/wear/java/tk/glucodata/Specific.java` — registers the now-shared trend provider at Wear startup.
- `Common/src/wear/java/tk/glucodata/ui/screens/MainScreen.kt` — pins the hero under the clock, stacks the selection headline beneath it, and places the range chip after the sensor card.
- `Common/src/wear/java/tk/glucodata/ui/screens/ChartScreen.kt` — removes the Raw preference/toggle, derives primary and secondary lanes from the canonical sensor view mode, retains a range-only bottom overlay on the dedicated route, and reduces target-band alpha to `0.06`.
- `Common/src/wear/java/tk/glucodata/ui/screens/SensorScreen.kt` — adds the Auto / Raw / Auto+Raw / Raw+Auto cycling row and writes `ManagedSensorViewModeStore`, the live managed driver, and native view mode consistently.

## Relocations

- `Common/src/mobile/java/tk/glucodata/logic/TrendEngine.kt` → `Common/src/main/java/tk/glucodata/logic/TrendEngine.kt`.
- `Common/src/mobile/java/tk/glucodata/logic/TrendEngineVelocityProvider.kt` → `Common/src/main/java/tk/glucodata/logic/TrendEngineVelocityProvider.kt`.
- `Common/src/mobile/java/tk/glucodata/ui/util/GlucoseFormatter.kt` → `Common/src/main/java/tk/glucodata/ui/util/GlucoseFormatter.kt`.
- `tk.glucodata.ui.GlucosePoint` moved from the mobile `GlucoseDisplay.kt` file into its own `src/main` file without changing its FQN or fields.

The relocated `TrendEngine.kt` compares byte-for-byte equal with the prior file (`cmp` exit 0); no algorithm line changed.

## Gates

- `git diff --check`: passed.
- `:Common:assembleWearLibre3SiDexGoogleDebug --no-daemon`: blocked before project configuration by the sandbox. The shared Gradle home first failed opening its `.lck` file with `Operation not permitted`. With Android Studio JBR and isolated `GRADLE_USER_HOME=/tmp/codex-juggluco-gradle` (using the locally cached Gradle distribution), Gradle still could not create `FileLockContentionHandler`: `java.net.SocketException: Operation not permitted`.
- `:Common:assembleMobileLibre3SiDexGoogleDebug --no-daemon`: same sandbox `FileLockContentionHandler` / `SocketException: Operation not permitted` before project configuration.

## Skipped

- No requested implementation item was skipped.
- Compile/link verification could not run because the sandbox blocks Gradle's local lock-contention socket service; neither gate reached source compilation.
